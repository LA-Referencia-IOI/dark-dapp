# dARK 2.0

![dARK Logo](docs/figures/dARK_logo.png)

**dARK** (Decentralized Archival Resource Key) is a blockchain-based implementation of the [ARK](https://arks.org/) identifier scheme.

**dARK is public, federated digital infrastructure.** It ensures permanent, decentralized access to identifiers while preventing proprietary appropriation of the core protocol.

[![DOI](https://zenodo.org/badge/DOI/10.5281/zenodo.7442743.svg)](https://doi.org/10.5281/zenodo.7442743)

---

## Documentation

- 📖 **[Developer Guide](DARK_2.0_GUIDE.md)**: Start here — usage, data structures, and lifecycle.
- 🏗 **[Architecture](DARK_2.0_ARCHITECTURE.md)**: System design and the Authority model.
- ⚙️ **[API Reference](DARK_2.0_API_REFERENCE.md)**: Contract functions, events, and error codes.

---

## Architecture Overview

dARK 2.0 uses a **two-contract modular architecture**, separating access control from data storage:

| Contract        | Role                                                                                        |
| --------------- | ------------------------------------------------------------------------------------------- |
| `Authority.sol` | Registers organizations (Authorities), binds UUIDs to wallets, and manages NAAN permissions |
| `dARK.sol`      | Pure ARK identifier storage — delegates all permission checks to `Authority.sol`            |

```
Admin ──► register_authority ──► Authority.sol ◄── is_authorized? ◄── dARK.sol ◄── create_ark ◄── Authority Wallet
```

---

## Project Structure

```
dARK/
├── config.ini                   # Network and account configuration
├── deploy.py                    # Compiles and deploys contracts
├── configure.py                 # Runs an end-to-end test of deployed contracts
├── clean.py                     # Cleanup script
├── requirements.txt             # Python dependencies
└── dARK_dapp/
    ├── contracts/
    │   ├── Authority.sol        # Access control contract
    │   ├── dARK.sol             # ARK storage contract
    │   └── IAuthority.sol       # Interface used by dARK.sol
    ├── compile.py               # Standalone Solidity compiler script
    ├── compiled/                # ABI and bytecode artifacts (generated)
    ├── deployed_contracts.ini   # Deployed addresses and ABIs (generated by deploy.py)
    └── test/
        └── test-dapp.ipynb      # Interactive test notebook
```

---

## Prerequisites

- Python 3.14
- A running [Hyperledger Besu](https://besu.hyperledger.org/) node (or compatible EVM node)

Install Python dependencies:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

## Configuration

Copy and edit the configuration file:

```bash
cp config.ini.example config.ini   # or edit config.ini directly
```

Key fields in `config.ini`:

```ini
[base]
blockchain_net = dark-local        # active network section

[dark-local]
url              = http://localhost:8545
chain_id         = 2025
account_priv_key = 0x...           # private key of the deployer / contract admin
```

> **Important:** The account used in `account_priv_key` becomes the `admin` of the `Authority` contract. Only this account can call `register_authority`.

---

## Deployment

### 1. Compile contracts (optional — deploy.py handles this automatically)

```bash
cd dARK_dapp
python3 compile.py
```

Artifacts are saved to `dARK_dapp/compiled/`.

### 2. Deploy contracts

```bash
python3 deploy.py
```

This will:

1. Compile `Authority.sol` and `dARK.sol`
2. Deploy `Authority.sol` (sets the deployer as `admin`)
3. Deploy `dARK.sol`, passing the `Authority` address to the constructor
4. Save both addresses and ABIs to `dARK_dapp/deployed_contracts.ini`

### 3. Verify deployment

```bash
python3 configure.py
```

Runs a full end-to-end test: registers an authority, authorizes a NAAN, creates an ARK, and resolves it.

---

## Interactive Testing (Jupyter Notebook)

The notebook `dARK_dapp/test/test-dapp.ipynb` provides a full interactive workflow:

1. **Connect** to the Besu node using settings from `config.ini`
2. **Load** the deployed contracts from `deployed_contracts.ini`
3. **Create** two fresh authority wallets (`Account.create()`)
4. **Fund** each wallet with **10 ETH** from the pre-funded master wallet
5. **Register** both wallets as Authorities (admin signs)
6. **Authorize** a NAAN per authority (each wallet signs for itself)
7. **Create** ARK identifiers under each NAAN
8. **Resolve** and verify the stored ARK data

```bash
cd dARK_dapp/test
jupyter notebook test-dapp.ipynb
```

> **Note:** UUIDs and ARK names are generated with a Unix timestamp suffix (`authority-001-{ts}`), so the notebook is safe to re-run against the same blockchain state without conflicts.

---

## ARK Lifecycle (Summary)

```
1. Admin calls:      Authority.register_authority(uuid, wallet, enc_key)
2. Authority calls:  Authority.authorize_naan(naan)
3. Authority calls:  dARK.create_ark(naan, name, url, cid)
4. Anyone calls:     dARK.resolve(naan, name)  →  returns url
```

---

## License

### Software License

The dARK source code is licensed under the **GNU Affero General Public License v3.0 (AGPLv3)**.

- You are free to use, modify, and distribute the software without cost, provided that network services built on dARK also make their source code available (closing the "ASP loophole").
- Licensing follows the repository policy; no standalone license file is
  included in this component.

### Documentation License

Documentation and non-code assets are licensed under **Creative Commons Attribution 4.0 International (CC BY 4.0)**.

---

## Contributing

See **[CONTRIBUTING.md](CONTRIBUTING.md)** for details on how to propose changes.

---

## Links

- [ARK Alliance](https://arks.org/)
- [Hyperledger Besu](https://besu.hyperledger.org/)
- [DOI: 10.5281/zenodo.7442743](https://doi.org/10.5281/zenodo.7442743)
